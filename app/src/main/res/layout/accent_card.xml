<?xml version="1.0" encoding="utf-8"?>
<!-- One accent theme as a value tile (the 8dp radius of the app's value controls, not a card of its
     own — it already sits on the section's card): a preview in the theme's ground for the appearance
     in force, carrying a dot of its fill, the name under it. A theme changes the ground as well as
     the accent, so the tile shows both. Chosen = a 2dp edge and the name in the fill; focused = the
     ring every control wears (Utils.focusRing's rule, restated in SettingsActivity for a card).

     A fixed width, not a weight: a weight inside a HorizontalScrollView measures to nothing. 116dp
     plus the 4dp margins is a 124dp pitch — 2.8 tiles on a 360dp phone, so the cut third says the row
     scrolls. -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="116dp"
    android:layout_height="wrap_content"
    android:layout_marginStart="4dp"
    android:layout_marginEnd="4dp"
    android:checkable="true"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@android:color/transparent"
    app:cardCornerRadius="8dp"
    app:cardElevation="0dp"
    app:cardForegroundColor="@android:color/transparent"
    app:checkedIcon="@null"
    app:rippleColor="@color/ripple_button"
    app:strokeColor="?attr/colorOutlineVariant"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="36dp">

            <View
                android:id="@+id/accent_ground"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_accent_swatch" />

            <View
                android:id="@+id/accent_dot"
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:layout_gravity="start|center_vertical"
                android:layout_marginStart="12dp"
                android:background="@drawable/bg_accent_dot" />
        </FrameLayout>

        <TextView
            android:id="@+id/accent_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:ellipsize="end"
            android:gravity="center_horizontal"
            android:maxLines="1"
            android:textAppearance="?attr/textAppearanceLabelMedium"
            android:textColor="?attr/colorOnSurface" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
