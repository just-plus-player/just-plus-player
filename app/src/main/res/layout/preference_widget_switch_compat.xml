<?xml version="1.0" encoding="utf-8"?>
<!-- Overrides androidx.preference's own copy, which inflates a SwitchCompat: that is the pre-M3
     switch — a narrow track with a small thumb hanging over its edges. MaterialSwitch is the Material 3
     one, and being a SwitchCompat subclass it keeps the id and the binding the library expects. Off also
     carries a cross on a full-size thumb, so it reads without telling two shades apart. -->
<com.google.android.material.materialswitch.MaterialSwitch
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/switchWidget"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:focusable="false"
    android:clickable="false"
    android:background="@null"
    app:thumbIcon="@drawable/switch_thumb_icon"
    app:thumbIconSize="16dp" />
